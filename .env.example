# Server Configuration
HOST=0.0.0.0
PORT=8080
ENVIRONMENT=development

# JWT Configuration
JWT_SECRET_KEY=your-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# Device Tokens (for hub authentication)
DEVICE_TOKEN_RPI_BRIDGE_01=dev-token-rpi-bridge-01
DEVICE_TOKEN_RPI_BRIDGE_02=dev-token-rpi-bridge-02

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:4173,http://localhost:5173,http://localhost:8080,https://gui.cornellhyperloop.com
